# Crawlberg runtime migration — 2026-08-12

Source snapshot: `/Users/junix/crawlberg` at
`e855adfa5a22bef61ba747f3b56ad4fb27fe4b5b`.

## Migrated core behavior

- Isolated in-memory BFS/DFS frontier with URL deduplication.
- Bounded page/depth/concurrency/link fan-out limits.
- Same-domain and optional subdomain/path scope policies.
- Per-domain request reservations, robots crawl-delay, and adaptive 429 backoff.
- Manual redirects, retry bounds, response byte limits, no-proxy requests, DNS
  address pinning, and private-network SSRF rejection.
- IPv4-mapped IPv6 and RFC 6052 NAT64 normalization before SSRF classification.
- robots.txt allow/disallow selection with specific-agent precedence.
- Full-page `nofollow` and per-link `nofollow` handling.
- Crawl events, failures, statistics, normalized result models, and a JSON CLI.

## Deliberate architectural change

Crawlberg performs fetch, link extraction, metadata extraction, and content
conversion inside one product model. `xcrawl` instead fetches once and passes a
`readabilities_rs::PageSnapshot` to `Reader::analyze_snapshot`.

The returned `PageAnalysis` contains full-page links independently from the
article result. Consequently a navigation/index page can return `no_content`
for its article while still advancing the crawl frontier.

The dependency direction is one-way: `xcrawl -> readabilities-rs`.
`readabilities-rs` has no knowledge of crawl queues, depth, concurrency,
robots.txt, or storage.

## Verification

- `xcrawl`: formatting, Clippy with warnings denied, all-target tests, and build
  pass under `just check-all`.
- The real-TCP crawl test proves that an HTML page with no readable article can
  still expose a link that advances the frontier.
- A CLI smoke test against Crawlberg's `basic_page.html` fixture crawled one
  page with zero failures and extracted the expected title.
- The dependency tree enables only `readabilities-rs/charset`; the
  `readabilities-rs/http` acquisition feature is not linked into `xcrawl`.
- The existing offline conformance suite remains 17/17 for `readabilities-rs`,
  with 0 security failures.
